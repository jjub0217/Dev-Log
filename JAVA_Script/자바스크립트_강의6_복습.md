# 함수란?

함수 표현식: 함수 리터럴이 온다. 함수 리터럴이 오면 이름을 붙여도 되고 안붙여도 된다. 일반적으로는 안쓴다. 왜 안붙일까? 헷갈리잖아. 



파스칼 케이스(앞에가 대문자)로 되어있는 영어는 전부 생성자 함수 또는 클래스라고 한다. 자스가 우리한테 쓰라고 주는 함수다. 얘네들은 객체를 생성용 함수다. (클래스도 함수다)



함수는 식별자로 호출하는 것이다. 자기 자신을 호출해야 할 필요가 있다고 하면(=재귀)

자기가  자기를 호출하는거다. 자기가 자기를 호출하면 또 걔가 나를 호출하고, 결국엔 무한루프가 된다. 그래서 반드시 탈출조건이 있어야 한다. 탈출조건을 잘못 만들면 스택 오버 플로어라는 에러가 난다. 재귀를 웬만해서는 안쓴다 . 어떨때 쓴다? 포문이랑 똑같다. 알고리즘에서 쓴다. 또는 예를 들어서 몇번 반복해야 할지 모를때, 예를 들어서 딥카피 같은 경우다. 프로퍼티 안에 들어갔는데 또 프로퍼티 안의 값이 객체고 이렇게 몇번 반복해야 할지 모를때 재귀를 쓰고 그 외에는 와일문으로 루프를 고치면 된다. 

 

생성자 함수는 쓸일도 없을거고, 써도 안된다. 쓰면 안된다. 그럼 왜 있을까? 됏어.  



화살표 함수는 우리가 나중에 집중적으로 공부할거야. 꽤나 중요해서. 왜냐면 얘는 일반 함수랑 여러 측면에서 다르다. 



## 함수 호출

매개변수는 몇개까지 지정할수 있을까? 몰라요. 몇백개는 충분히 가능하지만 그럴 사람이 있을까? 없어~~~ 

우리가 함수를 만들때 매개변수를 정의를 하겠지. 

매개변수가 많아진다고 가정하자. 7개 8개라고 하면 생각을 다시 해봐야한다. 이 함수를 하나로 만든다는 생각을 하지말자. 매개변수가 8개면 그만큼 재료가 많다는 뜻이다. 그럼 그만큼 많은 일을 한다는 뜻이다. 많은 일을 한다는 건 그럼 걔는 하나의 함수가 아닐 가능성이 있다. 함수의 단위를 어케 설정할건인지 생각해야 한다. 

함수는 하나의 일만 하는거다. 그래야 문이 이쁘게 나온다. 함수바디를 만들때 몇줄이 제일 좋을까?

많아야 한줄이 최고다. 한줄이 최고다. 그만큼 함수는 작게작게 만드는 것이 좋다. 특히 자스는 더 그렇다. 콜백이라는게 잇으니까. 인수가 4개 이상된다고 하면 다시 생각해보자.  꼭 4개가 필요하나? 하나의 함수로 만들어야 하나? 또하나의 팁은 이름 짓기가 애매하면 일단 생각을 다시 해보자. 함수의 단위를 다시 생각해보자. 이름이 잘 지어져야 한다. 잘 지어진다는 뜻은 함수가 하는 일이 명확해진다는 뜻이다. 



매개변수는 결론부터 말하면 0개가 최고다. 왜냐면 0개 일때 함수 사용법이 제일 쉽기 때문이다. 우리하 함수를 정의할때는 함수를 쓰는 사람을 고려해줘야 한다. 함수의 매개변수가 4개면 4개의 순서도 의미가 있다. 그러면 인수도 순서대로 3개를 나열해줘야 한다. 10개면 10개를 순서대로 나열해줘야한다. 하나만 삐끗하면, 인수를 하나를 잘못 주면 함수가 이상하게 도는거다. 인수가 많아지면 실수가 발생할 확률이 높아지는거고 사용하기에도 어려워진다. 매개변수는 3개를 넘지 않도록 노력하자. 

3개가 넘으면 이걸 객체로 주면 어떨까 라는 생각을 하면 된다. 우리가 지금 계속 인수를 원시값으로만 주고 있는데, 객체는 복합적인 자료구조이기 때문에 인수가 100개가 되건 하나의 값으로 줄수 잇다. 매개변수는 하나만 주면 된다. 프로퍼티의 순서에 의미가 있나? 객체에 프로퍼티가 있을텐데 순서가 의미가 없다.프로퍼티는 순서에 의미가 없기때문에 프로퍼티 키가 있는것이다. 프로퍼티 키가 값을 설명해주는 거기 때문에 가독성이 좋은거다. 프로퍼티 키를 잘 지엇다면 이 값이 의미하는 바가 명확해지는것이다.  

그리고 순서에 의미가 없고 우리가 전달해줘야 할 키와 값만 정확히 선언해주면 객체를 만들어주면 개는 내부적으로 다 값을 가져올수 있다. 함수는. 4개 이상이 되면 객체로 넘겨줄 생각을 하자.

근데 객체로 넘길거면 조심해야 한다. 



제이쿼리가 나쁜걸까? 안쓸까? 아주 간단한 웹사이트 만들때는 제이쿼리 쓴다. 근데 복잡한 애플리케이션 쓸때는 프레임워크 쓰는게 낫다. 



# 반환문







## 참조에 의한 전달과 외부 상태의 변경

```javascript

function changeVal(primitive, obj) {
  primitive += 100;
  obj.name = 'Kim';
}

// 외부 상태
var num = 100;
//var person = { name: 'Lee' };//

// 원시값은 값 자체가 복사되어 전달되고 객체는 참조값이 복사되어 전달된다.
changeVal(num, { name: 'Lee' });

```

원시값이 전달된다. 할당된다라고 이해해야 한다. 변수를 변수에 할당한거다.

원시값을 할당한 변수를 변수에 할당했다. 어떤 방식으로 한다? 패스 바이 벨류다. 

원시값이 복사되어서 전달되어진다. 



호출을 하면 컨트롤이 밑으로 가는게 아니라 함수내부로 간다. 함수내부로 들어와서 한줄한줄 실행될텐데 한줄한줄 실행되기 이전에 선처리 하는게 있다. 이 안에도 변수 선언이 잇을수도 있다. 

프리미티브 라는 변수를 선언하고 그리고 언디파인드를 할당하고 오비제이라는 변수를 선언하고 언디파인드를 할당한 상태에서 함수가 시작되는 것이다.

근데 지금 프리미티브가 지금 언디파인드 상태잖아. 근데 인수가 있다. 그래서 할당이 이루어진다.

그래서 프리미티브 는 넘 이 있는 것과 똑같다. 그러면 넘에 할당되어진 100 이란 원시값이 함수 내부에 있었잖아. 이게  이 함수 안으로 들어간다. 들어갈때 복사되어서 들어간다. 

 X= 1

var copy = x

함수 내부에서 함수 외부에 있는 값을 고쳤다. 

객체를 매개변수를 통해서 전달하면 함수 외부에 잇는 객체를 함수 내부에서 고칠수가 있다.

 지금 이 객체 리터럴을 변수에 할당했죠.  그래서 이 변수를 여기다 넣었잔항. 이렇게 안해도 된다고.

이 객체 리터럴을 이렇게 써도 상관없다고. 차이가 뭐냐 

```javascript
function foo(x) {
x = x + 1;
}
var x = 1;
foo(a);
```

```javascript
비순서 함수
function foo(x) {
x = x + a;
}
var x = 1;
var a - 10;
foo(a);   -> foo 함수는 .... x  값에 의존하고 있는 것이다. x 값이 바뀌면 함수의 리턴값도 바뀐다. 동일한 인수값을 전달해도 ....
console.log(x);  //  11
```



순서함수는 수학적함수다. 

수학점 함수는...

프로그래밍은 외부값에 의존하게 되면 외부값이 바뀌면 리턴값이 바뀐다. 

외부에 있는 값을 직접 고쳐가는 애들도 비순서 함수라고 한다. 

그래서 함수형 프로그래밍 언어라고 있는데 거기서 순수함수를 지향 한다. 백퍼센트는 아니더라도 가급적 순수함수를 만들어야 겟다. 가 함수형 프로그래밍인다. 



 

## 다양한 함수의 형태

### 즉시 실행 함수

함수긴 함수인데 즉시 실행한다. 정의 되자 마자 즉시 실행된다는 뜻이다. 

```javascript
// 익명 즉시 실행 함수
(function () {   -> 여기서 ( ) 이게 이름이 잇다고 하면 두가지 해석이 잇다. 
코드블록 안에 값이 와야 한다. 코드 블록 안은 함수 리터럴이다. 
  var a = 3;
  var b = 5;
  return a * b;
}());-> ( ) 이건 호출 연산자다. 
```



var 키워드인 경우에 중복을 허용한다.

```javascript
ex)
var x =1;
var x = 10;
```

```
(function () {
  // ...
}()); -> 이렇게 써도 된다.

(function () {
  // ...
})();   -> 이렇게 써도 된다. 이게 좋을거 같다. 

!function () {
  // ...
}();

+function () {
  // ...
}();
```



## 재귀 함수

재귀 함수: 함수 바디에서 함수 몸체에서 자기를 호출하는 문이 있으면 그걸 재귀함수라고 한다.

탈출을 안하면 무한반복 하게 된다. 



## 중첩 함수 (중요하다)

콜백 함수와 친척이다.

함수가 중첩됬다는 뜻. 함수 안에 함수를 선언하였다는 뜻이다. 

```javascript
function outer() {
  var x = 1;

  // 중첩 함수
  function inner() {
    var y = 2;
    // 외부 함수의 변수를 참조할 수 있다.
    console.log(x + y); // 3
  }

  inner();
}

outer();
```

컴퓨터 함수에서는 중첩이라는 뜻은 '부자' 관계라고 한다. (= '부모'관계)

의미론적 부자관계라고 하는데, .... 스코프가 연결되는데, .... 자식같은 개념이다. 

외부 함수의 헬퍼 역할을 한다. 외부 함수가 뭘 할건데, 중첩함수가 도와준다. 일의 일부를 중첩함수가 한다. 

그런데 클로저랑 밀접한 관계가 있다. 



## 콜백 함수

반복을 하는 함수 

포문을 내포하고 있는 함수를 만든다. 

인수로 숫자를 주면 그 숫자만큼 반복한다. 안에서 포문이 그 만큼 반복한다. 

근데 이 함수에는 문제가 잇다. 왜 반복을 하지? 반복을 하면서 뭔가를 해야 하잖아. 콘솔로그가 반복하면서 하는일이다. 포() 랑 콘솔로그 부분이 반복한다. 리핏()은 조절할수 잇는데 포()콘솔로그 는 조절할수 없다. 

횟수를 인주로 준것처럼 반복 하는 일이 바뀔수 잇잖아. 

 리핏 계열 함수 : 반복한다, 반복하면서 뭔가 한다. 반복한다는 것은 정해진것이다. 안 바뀜. 반복하면서 하는 일만 바뀜 반복하면서 하는 일만 외부에서 전달해주면 여러가지 일을 하나의 함수로 할수 있다.

함수가 할 일이다. 함수는 동사잖아. 

골백함수는 사용빈도가 많다.



예습 : 13. 14, 15 예습





























